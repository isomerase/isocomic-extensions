{
  // this file does nothing about compiling the ts files
  // it is used by vscode to find the ts files
  "extends": "./tsconfig.build.json",
  "include": [
    "src",
    "types",
    "utils"
  ]
}